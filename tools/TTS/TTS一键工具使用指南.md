# TTS一键式傻瓜工具使用指南

## 🎯 概述

这是一个完全自动化的文本转语音工具，具有以下特点：
- ✅ **零配置**：自动检查Python版本
- ✅ **自动虚拟环境**：自动创建tts_venv虚拟环境
- ✅ **自动安装依赖**：自动安装TTS和相关依赖
- ✅ **一键运行**：一条命令完成所有操作

## 🚀 使用方法

### 方法1：Python版本（推荐）
```bash
python3 tts_foolproof.py "你的文本"
```

### 方法2：Shell版本（更简单）
```bash
./tts_easy.sh "你的文本"
```

## 📋 使用示例

### 基本使用
```bash
./tts_easy.sh "Hello world"
python3 tts_foolproof.py "Hello world"
```

### 指定输出文件
```bash
./tts_easy.sh "你好世界" -o my_audio.wav
python3 tts_foolproof.py "你好世界" -o my_audio.wav
```

### 指定模型
```bash
./tts_easy.sh "Hello" --model-name tts_models/en/ljspeech/vits
python3 tts_foolproof.py "Hello" --model-name tts_models/en/ljspeech/vits
```

## 🛠️ 功能特点

### 1. 虚拟环境管理
- 自动检测是否存在`tts_venv`虚拟环境
- 如果不存在，自动创建虚拟环境
- 自动激活虚拟环境并管理依赖

### 2. 依赖管理
- 自动检查TTS和相关依赖是否已安装
- 如果未安装，自动安装以下依赖：
  - `TTS>=0.21.0,<0.23.0`
  - `numpy<2.0.0`
  - `torch>=2.0.0,<2.9.0`
  - `torchaudio`

### 3. 智能错误处理
- Python版本检查（需要3.8+）
- 网络连接问题提示
- 依赖安装失败提示
- 清晰的错误信息

### 4. 文件管理
- 自动生成音频文件名（如果未指定）
- 显示生成的文件路径
- 支持自定义输出路径

## 📁 文件结构

运行后会在当前目录下创建：
- `tts_venv/` - Python虚拟环境
- `*.wav` - 生成的音频文件

## ⚡ 性能特点

- **首次运行**：需要下载模型和安装依赖（较慢）
- **后续运行**：使用已安装的依赖和模型（很快）
- **处理时间**：通常0.2-0.5秒（取决于文本长度）

## 🔧 系统要求

- Python 3.8+
- 网络连接（首次安装依赖时）
- 约2GB磁盘空间（虚拟环境+模型）

## 🎨 输出示例

```
🎵 TTS一键式傻瓜脚本 🎵
🎤 完全自动化的文本转语音工具
ℹ️ [步骤] 检查Python安装...
✅ 成功: Python 3.9 检测通过
ℹ️ [步骤] 设置虚拟环境...
✅ 成功: 虚拟环境已存在
ℹ️ [步骤] 检查TTS依赖...
✅ 成功: TTS依赖已就绪

🚀 开始语音生成...
ℹ️ [步骤] 生成语音: 'Hello world'
✅ 成功: 语音生成成功！
ℹ️ 信息: 生成文件: 📁 hello_world.wav
✅ 任务完成！享受你的语音文件吧！
```

## 🆘 故障排除

### Python版本问题
如果提示Python版本过低，请安装Python 3.8+：
```bash
# macOS
brew install python3

# Ubuntu/Debian
sudo apt update && sudo apt install python3 python3-pip python3-venv
```

### 网络问题
如果依赖安装失败，请检查网络连接，或使用国内镜像：
```bash
# 设置pip国内镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

### 权限问题
如果遇到权限问题，请确保当前目录有写权限。

## 🎯 最佳实践

1. **首次使用**：耐心等待依赖安装完成
2. **批量转换**：可以编写脚本批量处理多个文本
3. **文件管理**：建议将音频文件保存到专门的目录

## 📞 支持

如有问题，请检查：
1. Python版本是否正确
2. 网络连接是否正常
3. 磁盘空间是否充足
4. 当前目录权限是否正确